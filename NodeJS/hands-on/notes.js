console.log("hello world");
// to run in node environment "node notes.js"

// console.log(document); // we get a reference error because there is no dom object in node

// we do get access to node REPL which is 'Read Evaluate Print Loop' which allows us to write JS in the terminal, by typing and entering 'node'

// at the heart of every nodejs project, we got the package.json file
// package.json is the blueprint
// it contains the metadata (name, version, author, description, etc.)
// simplifies collaboration (manages dependencies, defining a start script)

// command: npm init
// it creates a package.json file, but we need to enter everything manually

// command: npm init -y
// it writes a boilerplate version
